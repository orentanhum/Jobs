import { useEffect,useState } from 'react'
import { Save,Upload,FileText,UserRound,Target } from 'lucide-react'
import { supabase } from './supabase'

type Profile={full_name?:string;email?:string;phone?:string;linkedin_url?:string;location?:string;current_title?:string;professional_summary?:string;target_roles?:string[];preferred_locations?:string[];work_modes?:string[];keywords?:string[];preferred_companies?:string[];excluded_companies?:string[];excluded_keywords?:string[];minimum_fit?:number;auto_discovery?:boolean;cv_file_name?:string;cv_storage_path?:string;cv_updated_at?:string}
const csv=(v?:string[])=>v?.join(', ')||''; const arr=(v:string)=>v.split(',').map(x=>x.trim()).filter(Boolean)
export default function SettingsPage(){const[p,setP]=useState<Profile>({});const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user)return;const{data}=await supabase.from('profiles').select('*').eq('id',user.id).single();if(data)setP(data)})()},[]);const set=(k:keyof Profile,v:any)=>setP(x=>({...x,[k]:v}));const save=async()=>{setBusy(true);setMsg('');const{data:{user}}=await supabase.auth.getUser();if(!user)return;const{error}=await supabase.from('profiles').update({...p,updated_at:new Date().toISOString()}).eq('id',user.id);setMsg(error?error.message:'Settings saved');setBusy(false)};const upload=async(f?:File)=>{if(!f)return;setBusy(true);const{data:{user}}=await supabase.auth.getUser();if(!user)return;const path=`${user.id}/${Date.now()}-${f.name}`;const{error}=await supabase.storage.from('cvs').upload(path,f);if(error){setMsg(error.message);setBusy(false);return}await supabase.from('profiles').update({cv_file_name:f.name,cv_storage_path:path,cv_updated_at:new Date().toISOString()}).eq('id',user.id);setP(x=>({...x,cv_file_name:f.name,cv_storage_path:path,cv_updated_at:new Date().toISOString()}));setMsg('CV uploaded');setBusy(false)};const field=(label:string,k:keyof Profile,placeholder='')=><label className="block"><span className="text-xs font-bold text-slate-600">{label}</span><input value={(p[k] as string)||''} onChange={e=>set(k,e.target.value)} placeholder={placeholder} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label>;const list=(label:string,k:keyof Profile,placeholder:string)=><label className="block"><span className="text-xs font-bold text-slate-600">{label}</span><input value={csv(p[k] as string[])} onChange={e=>set(k,arr(e.target.value))} placeholder={placeholder} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/><span className="text-[11px] text-slate-400">Separate with commas</span></label>;return <div className="max-w-4xl"><div className="flex items-start gap-3"><div><h1 className="text-2xl font-bold">Settings</h1><p className="text-sm text-slate-500">Your profile drives private job tracking, discovery and matching.</p></div><button onClick={save} disabled={busy} className="ml-auto flex items-center gap-2 rounded-lg bg-indigo-600 px-4 py-2 text-sm font-bold text-white"><Save size={16}/>{busy?'Saving…':'Save'}</button></div>{msg&&<div className="mt-3 rounded-lg bg-indigo-50 p-3 text-sm text-indigo-700">{msg}</div>}<section className="mt-4 rounded-xl border bg-white p-4"><h2 className="mb-4 flex items-center gap-2 font-bold"><UserRound size={18}/>My Profile</h2><div className="grid gap-3 md:grid-cols-2">{field('Full name','full_name')}{field('Current title','current_title')}{field('Email','email')}{field('Phone','phone')}{field('Location','location')}{field('LinkedIn','linkedin_url','https://linkedin.com/in/...')}</div><label className="mt-3 block"><span className="text-xs font-bold text-slate-600">Professional summary</span><textarea value={p.professional_summary||''} onChange={e=>set('professional_summary',e.target.value)} rows={4} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label></section><section className="mt-4 rounded-xl border bg-white p-4"><h2 className="mb-4 flex items-center gap-2 font-bold"><Target size={18}/>Jobs I'm Looking For</h2><div className="grid gap-3 md:grid-cols-2">{list('Target roles','target_roles','Program Manager, Delivery Manager')}{list('Preferred locations','preferred_locations','North, Sharon, Tel Aviv')}{list('Work modes','work_modes','Hybrid, Remote')}{list('Skills / keywords','keywords','AI, DevOps, SaaS')}{list('Preferred companies','preferred_companies','Microsoft, Mobileye')}{list('Excluded companies','excluded_companies','')}{list('Excluded keywords','excluded_keywords','') }<label><span className="text-xs font-bold text-slate-600">Minimum fit %</span><input type="number" min="0" max="100" value={p.minimum_fit??70} onChange={e=>set('minimum_fit',Number(e.target.value))} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="flex items-center gap-3 pt-6 text-sm"><input type="checkbox" checked={p.auto_discovery??true} onChange={e=>set('auto_discovery',e.target.checked)}/>Automatically discover matching opportunities</label></div></section><section className="mt-4 rounded-xl border bg-white p-4"><h2 className="mb-2 flex items-center gap-2 font-bold"><FileText size={18}/>My CV</h2><p className="mb-3 text-sm text-slate-500">Upload PDF or Word, up to 10 MB. Your CV is private to your account.</p>{p.cv_file_name&&<div className="mb-3 rounded-lg bg-slate-50 p-3 text-sm"><b>{p.cv_file_name}</b><div className="text-xs text-slate-400">Uploaded {p.cv_updated_at?new Date(p.cv_updated_at).toLocaleString():''}</div></div>}<label className="inline-flex cursor-pointer items-center gap-2 rounded-lg border border-indigo-300 px-4 py-2 text-sm font-bold text-indigo-700"><Upload size={16}/>{p.cv_file_name?'Replace CV':'Upload CV'}<input className="hidden" type="file" accept=".pdf,.doc,.docx" onChange={e=>upload(e.target.files?.[0])}/></label></section></div>}
